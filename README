An interactive map for (initially) Crusader Kings II.

The map runs on leaflet.js.

Subfolders:
-paradomap: contains the map files
-marcher: a component for generating a GeoJSON file from a RGB bitmap that comes with
	  Crusader Kings II (provinces.bmp)